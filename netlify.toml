[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so deep links (e.g. /cases/case-shoulder-1) resolve to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
